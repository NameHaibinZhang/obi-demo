syntax = "proto3";

package obidemo;

service DemoService {
  rpc GetData (DataRequest) returns (DataResponse);
  rpc GetDataError (DataRequest) returns (DataResponse);
}

message DataRequest {
  string request_id = 1;
}

message DataResponse {
  string data = 1;
  string source = 2;
}